{
  "name": "bigman",
  "version": "0.1.0",
  "private": true,
  "description": "Personalized prop firm challenge finder \u2014 a recommendation engine, not a directory.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "db:reset": "tsx db/reset.ts",
    "db:seed:demo": "tsx db/seed-demo.ts",
    "test": "vitest run",
    "demo": "npm run db:reset && npm run db:seed:demo && next dev",
    "demo:build": "npm run db:reset && npm run db:seed:demo && next build && next start",
    "catalogue": "npm run catalogue:build && next dev",
    "catalogue:build": "npm run db:reset && npm run db:futures -- data/futures-specs.json data/futures-catalogue.csv && npm run db:futures -- data/cfd-specs.json data/cfd-catalogue.csv && npm run db:import -- data/futures-catalogue.csv --apply && npm run db:import -- data/cfd-catalogue.csv --apply && npm run db:import -- data/propfirmmatch-import.csv --apply && npm run db:firm-websites -- --apply && npm run db:firm-profiles -- --apply && npm run db:publish:all",
    "db:import": "tsx db/import.ts",
    "db:convert": "tsx db/convert-propfirmmatch.ts",
    "db:expand-sizes": "tsx db/expand-sizes.ts",
    "db:firm-websites": "tsx db/apply-firm-websites.ts",
    "db:research": "tsx db/apply-research.ts",
    "db:prices": "tsx db/apply-price-table.ts",
    "db:rules": "tsx db/apply-rules-table.ts",
    "db:platforms": "tsx db/apply-platforms.ts",
    "db:gaps": "tsx db/export-gaps.ts",
    "db:research:rules": "tsx db/build-rules-research.ts",
    "db:firm-profiles": "tsx db/apply-firm-profile.ts",
    "db:publish:all": "tsx db/publish-all.ts",
    "db:template": "tsx db/import.ts --template",
    "db:futures": "tsx db/build-futures-catalogue.ts",
    "build:standalone": "bash tools/standalone/build.sh"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "next": "^16.3.1",
    "react": "^19.1.9",
    "react-dom": "^19.1.9",
    "server-only": "^0.0.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.10.5",
    "@types/react": "^19.0.7",
    "@types/react-dom": "^19.0.3",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
