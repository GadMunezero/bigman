/**
 * Test stub for the `server-only` package.
 *
 * The real module throws when imported outside a React Server Component, which
 * is exactly what makes it useful in the app and useless in a test runner. The
 * build-time guarantee is unaffected: `next build` still resolves the real one.
 */
export {};
